name: Build Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  java-build:
    name: Java Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '21'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Build and test
        run: ./mvnw verify -B

  native-build:
    name: Native Image Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '21'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Cache GraalVM native image build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/graalvm
            target/graalvm-reachability-metadata
            target/native
          key: ${{ runner.os }}-graalvm-native-${{ hashFiles('**/pom.xml', 'src/**') }}
          restore-keys: ${{ runner.os }}-graalvm-native-

      - name: Run native tests
        run: ./mvnw -PnativeTest test -B

      - name: Build native image
        run: ./mvnw -Pnative native:compile -B -DskipTests

      - name: Upload native executable
        uses: actions/upload-artifact@v4
        with:
          name: native-executable
          path: target/todo
          retention-days: 7
